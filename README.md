Project is incomplete 
TODO
- if multiple results come back for the same collector number use an image hash to determine which it is
- improve ui
- set it up on a server
- cache search results to potentially increase speed using redis
