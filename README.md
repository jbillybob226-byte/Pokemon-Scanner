Project is incomplete 
TODO
- get yolo to crop all images and pass them each to paddleocr
- get the ocr to read the collector number (fractions) at the bottom of the card
- use the fraction to call the api
- if multiple results come back for the same collector number use an image hash to determine which it is
- add a use camera feature
- improve ui
